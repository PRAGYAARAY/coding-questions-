PROBLEM STATEMENT:

Given an integer array `arr` and an integer `k`, partition the array into contiguous non-empty subarrays such that the length of each subarray is at most `k`.

The partitions are ordered sequentially from left to right and numbered `1, 2, 3, ..., P` (1-indexed):

* Odd-numbered partitions (`1st, 3rd, 5th, ...`): Every element in the subarray is replaced by the minimum element of that subarray.

* Even-numbered partitions (`2nd, 4th, 6th, ...`): Every element in the subarray is replaced by the maximum element of that subarray.

Return the maximum total sum of the array after choosing an optimal partitioning scheme.





### Example 1

* Input: `arr = [10, 10, 10, 1, 1000, 1000]`, `k = 3`

* Output: `3030`

Explanation:

1. Partition 1 (Odd): `[10, 10, 10]` → length = 3, min = 10 ⇒ sum = `3 × 10 = 30`

2. Partition 2 (Even): `[1, 1000, 1000]` → length = 3, max = 1000 ⇒ sum = `3 × 1000 = 3000`

Total Sum: `30 + 3000 = 3030`


### Example 2

* Input: `arr = [1, 15, 7, 9, 2, 5, 10]`, `k = 3`

* Output: `84`

Explanation:

1. Partition 1 (Odd): `[1]` → length = 1, min = 1 ⇒ sum = `1 × 1 = 1`

2. Partition 2 (Even): `[15, 7, 9]` → length = 3, max = 15 ⇒ sum = `3 × 15 = 45`

3. Partition 3 (Odd): `[2]` → length = 1, min = 2 ⇒ sum = `1 × 2 = 2`

4. Partition 4 (Even): `[5, 10]` → length = 2, max = 10 ⇒ sum = `2 × 10 = 20`

(Note: The actual optimal total depends on the exact partitioning; this example illustrates the structure.)
















# Why Greedy Fails (Counterexample)

Consider `arr = [10, 10, 10, 1, 1000, 1000]` with `k = 3`.

### Greedy Partitioning

1. Partition 1 (Odd): `[10]` → `1 × 10 = 10`
2. Partition 2 (Even): `[10, 10, 1]` → `3 × 10 = 30`
3. Partition 3 (Odd): `[1000]` → `1 × 1000 = 1000`
4. Partition 4 (Even): `[1000]` → `1 × 1000 = 1000`

Total = **2040**

### Optimal Partitioning

1. Partition 1 (Odd): `[10, 10, 10]` → `3 × 10 = 30`
2. Partition 2 (Even): `[1, 1000, 1000]` → `3 × 1000 = 3000`

Total = **3030**

Therefore, a locally optimal greedy choice does not guarantee a globally optimal answer.

---

# Dynamic Programming Approach

Let `N = len(arr)`.

Define:

* `dpOdd[i]` = maximum sum obtainable from suffix `arr[i:]` when the next partition is **odd-numbered**.
* `dpEven[i]` = maximum sum obtainable from suffix `arr[i:]` when the next partition is **even-numbered**.

## Base Case

When no elements remain:

```text
dpOdd[N] = 0
dpEven[N] = 0
```

## Transition

### Odd partition

For every valid length `len`:

```text
dpOdd[i] = max(
    len × min(arr[i : i+len]) + dpEven[i+len]
)
```

### Even partition

```text
dpEven[i] = max(
    len × max(arr[i : i+len]) + dpOdd[i+len]
)
```

---

# Bottom-Up Computation

We compute states from right to left (`i = N-1 ... 0`).

To avoid recomputing minima and maxima, maintain running values while extending the partition.

## Pseudocode

```python
N = len(arr)

dpOdd = [0] * (N + 1)
dpEven = [0] * (N + 1)

for i in range(N - 1, -1, -1):

    # Odd partition state
    mn = float('inf')
    best = 0
    for length in range(1, min(k, N - i) + 1):
        mn = min(mn, arr[i + length - 1])
        best = max(best, length * mn + dpEven[i + length])
    dpOdd[i] = best

    # Even partition state
    mx = -float('inf')
    best = 0
    for length in range(1, min(k, N - i) + 1):
        mx = max(mx, arr[i + length - 1])
        best = max(best, length * mx + dpOdd[i + length])
    dpEven[i] = best

answer = dpOdd[0]
```

---

# Correctness (Brief)

For each position `i`, every feasible first partition length from `1` to `k` is considered. The contribution of that partition is computed exactly:

* odd partition → `length × minimum`,
* even partition → `length × maximum`.

After choosing the first partition, the remaining suffix is solved optimally by the corresponding opposite-parity DP state. Since the recurrence takes the maximum over **all** valid first choices, `dpOdd[i]` and `dpEven[i]` store the optimal values for their respective states. By induction on the suffix length, the algorithm returns the optimal total sum.

---

# Complexity

* **Time Complexity:** `O(N × k)`
* **Space Complexity:** `O(N)`

This satisfies the constraints `N ≤ 5000`.


